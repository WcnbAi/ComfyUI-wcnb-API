# 基础依赖（所有环境必需）
numpy>=1.21.0,<2.0.0
Pillow>=10.1.0,<11.0.0
requests>=2.25.0,<3.0.0

# 可选依赖（ComfyUI环境需要，扣子平台不需要）
# 如果不需要torch，可以注释掉下面这行
torch>=1.9.0,<2.5.0

# 版本说明：
# - numpy < 2.0.0: 避免numpy 2.x的兼容性问题，torch需要numpy 1.x
# - Pillow < 11.0.0: 稳定版本，与torchvision兼容
# - requests < 3.0.0: 保持向后兼容
# - torch < 2.5.0: 避免最新版本的潜在兼容性问题